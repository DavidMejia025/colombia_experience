<% if (@quote) %>
  <%= render 'activities/partials/new_quote_activity', { quote: @quote, city_activities: @city_activities } %>
<% else %>
  <%= render 'activities/partials/new_city_activity', { activity: @activity } %>
<% end %>
