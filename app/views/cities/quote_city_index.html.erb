<div class="container">
  <div class="row">
    <h1> Escoge una ciudad:</h1>

    <% @cities.each do |city| %>
        <ul>
          <li> <a href = <%="/quotes/#{@quote.id}/cities/#{city.id}"%>> <%= city.name %> </a> </li>
        </ul>
    <% end %>
  </div>
</div>
