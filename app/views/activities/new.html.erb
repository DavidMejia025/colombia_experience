<% if (@quote) %>
  <%= render 'activities/partials/quote_activity', { quote: @quote, city_activities: @city_activities } %>
<% else %>
  <%= render 'activities/partials/city_activity', { activity: @activity } %>
<% end %>
